properties:
  payloadId:
    format: int64
    type: integer
  payloadStatus:
    properties:
      latestValidHash:
        properties:
          hi:
            properties:
              hi:
                format: int64
                type: integer
              lo:
                format: int64
                type: integer
            type: object
          lo:
            properties:
              hi:
                format: int64
                type: integer
              lo:
                format: int64
                type: integer
            type: object
        type: object
      status:
        enum:
          - - VALID
            - INVALID
            - SYNCING
            - ACCEPTED
            - INVALID_BLOCK_HASH
        type: string
      validationError:
        type: string
    type: object
type: object
